[project]
name = "nifi-kafka-log-lab"
version = "0.1.0"
description = "Synthetic service-log generator publishing JSON logs to Kafka for the NiFi log lab"
requires-python = ">=3.9"
dependencies = [
    "confluent-kafka==2.6.1",
]

[tool.uv]
package = false
